"""Software Production Engine — deterministic gate runner.

No model, no network, no randomness: the same source produces the same findings every run.
That determinism is the point. A check an agent can talk its way past is not a gate.
"""

__version__ = "0.1.0"
